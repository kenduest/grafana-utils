# ai-changes-archive-2026-03-31.md

Archived AI change-log entries moved out of the current change log.

- Source: `docs/internal/ai-changes.md`
- Archived on: `2026-04-02`
- Kept in the live file: 2026-04-01 and newer entries only.

## 2026-03-31 - Add explicit dashboard provisioning export provider overrides
- Summary: added export CLI knobs for the dashboard provisioning provider block so operators can override the provider name, org ID, path, deletion/update policy, and update interval while keeping the current default `grafana-utils-dashboards` YAML shape intact. The generated provider file still lands at `provisioning/provisioning/dashboards.yaml`, but its content is now explicitly operator-controlled instead of hardcoded.
- Tests: added focused parser/help coverage for the new provisioning-provider flags and a runtime regression that asserts the generated YAML contains the override values and the resolved provider path.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet dashboard_cli_parser_help_rust_tests -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet export_focus_report_path_top_rust_tests -- --test-threads=1`, `git diff --check -- rust/src/commands/dashboard/cli_defs_command.rs rust/src/commands/dashboard/export.rs rust/src/commands/dashboard/dashboard_cli_parser_help_rust_tests.rs rust/src/commands/dashboard/export_focus_report_path_top_rust_tests.rs rust/src/commands/dashboard/inspect_live.rs rust/src/commands/dashboard/mod.rs rust/src/commands/dashboard/inspect_live_export_parity_all_orgs_rust_tests.rs docs/user-guide.md`
- Impact: `rust/src/commands/dashboard/cli_defs_command.rs`, `rust/src/commands/dashboard/export.rs`, `rust/src/commands/dashboard/dashboard_cli_parser_help_rust_tests.rs`, `rust/src/commands/dashboard/export_focus_report_path_top_rust_tests.rs`, `rust/src/commands/dashboard/inspect_live.rs`, `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/dashboard/inspect_live_export_parity_all_orgs_rust_tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and preserves current defaults, but future export help should keep the provider override flags explicit so the generated provisioning YAML does not drift back into a hardcoded stub.
- Follow-up: none.

## 2026-03-31 - Reword snapshot review as inventory review
- Summary: updated the snapshot CLI help, example blocks, and user-guide section so `snapshot review` is presented as a local inventory review over the exported dashboard and datasource root rather than as an overview/workbench alias. The runtime path itself stays thin; only the operator-facing contract text changed.
- Tests: added focused Rust help regressions for the snapshot subcommand help text and unified help examples.
- Test Run: pending local rerun in this task; record the exact `cli_rust_tests` command in the final validation report.
- Impact: `rust/src/commands/snapshot/mod.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is a wording-only contract cleanup, but future snapshot help should keep the inventory-review terminology aligned across CLI and docs instead of drifting back to overview/workbench language.
- Follow-up: none.

## 2026-03-31 - Add snapshot export/review wrappers
- Summary: added a new top-level `snapshot` namespace so the common all-org inventory flow no longer requires two export commands plus manually restating both staged input directories. `snapshot export` now fans into dashboard and datasource all-org exports under one `--export-dir`, and `snapshot review` renders a local inventory review against the derived `dashboards/` and `datasources/` children from `--input-dir`.
- Tests: added focused CLI parser/dispatch coverage for the new namespace and snapshot-specific runtime tests for path derivation, export wrapper fan-out, inventory review wiring, and `--overwrite` propagation.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet snapshot`, `cargo test --manifest-path rust/Cargo.toml --quiet cli_rust_tests`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_rust_tests`, `cargo test --manifest-path rust/Cargo.toml --quiet datasource`, `cargo test --manifest-path rust/Cargo.toml dashboard`, `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo fmt --manifest-path rust/Cargo.toml --all --check`
- Impact: `rust/src/commands/snapshot/mod.rs`, `rust/src/commands/snapshot/tests.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/lib.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The wrapper is intentionally thin over existing dashboard, datasource, and overview contracts, but future snapshot scope growth should stay explicit rather than silently adding access/user/team exports to v1.
- Follow-up: if operators later want access resources in the same flow, add them as explicit opt-in flags with clear scope semantics instead of silently broadening the default snapshot contract.

## 2026-03-31 - Make dashboard provisioning export provider path concrete
- Summary: changed the Rust dashboard provisioning export helper so the generated `provisioning/provisioning/dashboards.yaml` points at the exported dashboards directory instead of a generic placeholder. The path is resolved from the current export tree, with canonicalization when possible, so the emitted provider config is immediately usable on the export host.
- Tests: updated the dashboard provisioning export regression to assert the provider YAML contains the resolved dashboards directory path and no longer contains the placeholder marker.
- Test Run: pending focused Rust rerun in this task; record exact commands in the final validation report.
- Impact: `rust/src/commands/dashboard/export.rs`, `rust/src/commands/dashboard/export_focus_report_path_top_rust_tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and only affects the generated provisioning config path value, but future export work should preserve the explicit exported-dashboards-dir default rather than reintroducing a placeholder stub.
- Follow-up: none.

## 2026-03-31 - Add dashboard validate-export provisioning input format
- Summary: extended `dashboard validate-export` with the same explicit raw/provisioning split already used by dashboard import, diff, and inspect. Raw remains the default, while `--input-format provisioning` now validates dashboard JSON from either the `provisioning/` root or its `dashboards/` subdirectory.
- Tests: added validate-export parser/help coverage for the new input format and runtime coverage for provisioning-root and provisioning-dashboards validation runs.
- Test Run: `cargo check --manifest-path rust/Cargo.toml --lib`, `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_dashboard_validate_export_command -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_dashboard_validate_export_provisioning_input_format -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet validate_export_help_mentions_provisioning_input_format -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet run_dashboard_validate_export_supports_provisioning_root -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet run_dashboard_validate_export_supports_provisioning_dashboards_dir -- --test-threads=1`
- Impact: `rust/src/commands/dashboard/validate.rs`, `rust/src/commands/dashboard/cli_defs_command.rs`, `rust/src/commands/dashboard/cli_defs_inspect.rs`, `rust/src/commands/dashboard/dashboard_cli_inspect_help_rust_tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and keeps raw as the default lane, but future validation work should keep using the explicit input-format contract rather than guessing from path shape.
- Follow-up: none.

## 2026-03-31 - Add datasource diff provisioning input format
- Summary: added `--input-format provisioning` to `datasource diff`, wired diff loading through the shared datasource import resolver, and kept inventory as the default lane. Provisioning diff now accepts the export root, a `provisioning/` directory, or a concrete YAML file and compares the normalized datasource contract against live Grafana.
- Tests: expanded focused Rust coverage for diff help text, parser defaults, parser provisioning selection, inventory diff, and provisioning diff from root/directory/file forms.
- Test Run: `cargo test --quiet diff_ -- --test-threads=1` passed; `cargo test --quiet parse_datasource_diff_supports_provisioning_input_format -- --test-threads=1` blocked by an unrelated missing file in `rust/src/commands/dashboard/import_edge_routed_org_scope_rust_tests.rs`; `cargo test --quiet unified_help_full_appends_extended_examples -- --test-threads=1` blocked by the same repository issue; `cargo fmt --manifest-path rust/Cargo.toml --all` blocked by the same repository issue.
- Impact: `rust/src/commands/datasource/mod.rs`, `rust/src/commands/datasource/cli/defs.rs`, `rust/src/commands/datasource/import/support.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/commands/datasource/tests/diff.rs`, `rust/src/commands/datasource/tests/tail.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and leaves inventory as the default diff path, but the repo currently has a pre-existing missing test module that prevents a full cargo test/fmt pass until that unrelated file is restored.
- Follow-up: restore the missing dashboard test module before running a full crate validation pass.

## 2026-03-31 - Clarify Python dashboard diff raw/provisioning help split
- Summary: updated the Python dashboard diff help and unified dashboard help so they explicitly call out that the Python diff path stays on the raw replay lane and provisioning trees should be inspected separately. The examples now point Python provisioning users at `dashboard inspect-export --input-format provisioning` instead of leaving the lane split implicit.
- Tests: tightened focused Python dashboard help assertions to cover the raw/provisioning wording in both the dashboard CLI and unified dashboard help surfaces.
- Test Run: `PYTHONPATH=python poetry run python -m unittest -v python.tests.test_python_dashboard_cli.ExporterTests.test_dashboard_parse_args_supports_diff_mode python.tests.test_python_dashboard_cli.ExporterTests.test_dashboard_diff_help_mentions_raw_and_provisioning_lanes python.tests.test_python_dashboard_cli.ExporterTests.test_dashboard_top_level_help_includes_basic_and_token_examples python.tests.test_python_unified_cli.UnifiedCliTests.test_unified_parse_args_dashboard_without_subcommand_prints_dashboard_help python.tests.test_python_unified_cli_dashboard_capture.UnifiedDashboardCaptureCliTests.test_unified_cli_dashboard_capture_dashboard_group_help_mentions_new_commands`
- Impact: `python/grafana_utils/dashboard_cli.py`, `python/grafana_utils/unified_cli.py`, `python/tests/test_python_dashboard_cli.py`, `python/tests/test_python_unified_cli.py`, `python/tests/test_python_unified_cli_dashboard_capture.py`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is help and docs alignment only, so rollback is straightforward, but future Python dashboard diff docs should keep the raw/provisioning split explicit rather than reintroducing lane ambiguity.
- Follow-up: none.

## 2026-03-31 - Route dashboard provisioning imports with `--use-export-org`
- Summary: extended routed dashboard import scope discovery so `dashboard import --use-export-org --input-format provisioning` can consume the combined `--all-orgs` export root instead of stopping at the old raw-only gate. Each discovered `org_<id>_<name>/provisioning/` subtree now resolves export org identity from that provisioning export and then reuses the existing per-org rebinding path unchanged.
- Tests: added focused dashboard routed-import regressions for the successful combined-root provisioning path and for the single-org provisioning-root rejection message.
- Test Run: pending focused Rust rerun in this task; record exact commands in the final validation report.
- Impact: `rust/src/commands/dashboard/import_validation.rs`, `rust/src/commands/dashboard/rust_tests.rs`, `rust/src/commands/dashboard/import_edge_routed_org_scope_rust_tests.rs`, `rust/src/commands/dashboard/import_edge_routed_org_scope_provisioning_rust_tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to medium. The routed behavior now spans both raw and provisioning lanes, but the change stays in discovery/error handling and leaves the downstream per-org import flow untouched. Future work should keep this explicit `--input-format` split rather than reintroducing path guessing.
- Follow-up: if routed provisioning later needs richer validation than `index.json` plus export metadata provide today, extend the scoped export-org metadata loader directly instead of adding special cases in the live import loop.

## 2026-03-31 - Add dashboard diff provisioning input format
- Summary: added `--input-format provisioning` to `dashboard diff` and routed diff resolution through the same explicit dashboard source resolver used by import. Raw remains the default, while provisioning mode now accepts either the provisioning root or its `dashboards/` subdirectory and compares those JSON files against live Grafana without format guessing.
- Tests: added parser/help coverage for the new diff input format and runtime coverage for both provisioning entry points, while updating the existing raw diff fixtures to set the explicit raw format.
- Test Run: `cargo test --quiet diff_dashboards_with_client -- --nocapture`, `cargo test --quiet provisioning_diff -- --nocapture`, `cargo test --quiet diff_help_mentions_provisioning_input_format -- --nocapture`
- Impact: `rust/src/commands/dashboard/cli_defs_command.rs`, `rust/src/commands/dashboard/import.rs`, `rust/src/commands/dashboard/import_compare.rs`, `rust/src/commands/dashboard/export_diff_rust_tests.rs`, `rust/src/commands/dashboard/export_diff_tail_rust_tests.rs`, `rust/src/commands/dashboard/dashboard_cli_parser_help_rust_tests.rs`, `rust/src/commands/dashboard/import_edge_routed_org_scope_selection_create_rust_tests.rs`
- Rollback/Risk: low. The change is additive and keeps raw as the default path, but future diff work should preserve the explicit raw/provisioning split so the command does not drift back into format guessing.
- Follow-up: if diff needs more input contracts later, extend the explicit input-format enum rather than adding path heuristics.

## 2026-03-31 - Add dashboard provisioning staged-summary inputs to overview/status
- Summary: added an explicit `--dashboard-provisioning-dir` staged input to both `overview` and `status staged` so dashboard provisioning artifacts can be summarized without guessing at the raw replay contract. The new lane reuses the existing dashboard inspection summary path through a variant-aware helper, keeps `--dashboard-export-dir` as the default raw lane, and makes the two dashboard inputs conflict cleanly instead of silently competing.
- Tests: expanded focused overview and project-status parser/help coverage to assert the new provisioning flag, the raw/provisioning conflict, and the new staging input wiring.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet overview_rust_tests -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests -- --test-threads=1`
- Impact: `rust/src/commands/dashboard/inspect.rs`, `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/artifacts.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/overview/tests.rs`, `rust/src/commands/status/tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. The change is additive and keeps the raw dashboard lane intact, but future dashboard-summary work should preserve the explicit raw/provisioning split rather than reintroducing format guessing.
- Follow-up: if dashboard provisioning needs richer staged signals later, extend the same variant-aware helper instead of folding provisioning back into the raw export-dir contract.

## 2026-03-31 - Add dashboard provisioning export lane
- Summary: added a dedicated `provisioning/` dashboard export variant instead of overloading `raw/`. The new lane writes plain dashboard JSON under `provisioning/dashboards/`, emits provisioning variant metadata and root-index pointers, and generates a starter `provisioning/provisioning/dashboards.yaml` file for Grafana file provisioning.
- Tests: updated dashboard export/index regression coverage for the new variant directory and root-index shape, and updated helper/test call sites that construct `ExportArgs` or root-index values directly.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet build_export_variant_dirs_returns_raw_and_prompt_dirs`, `cargo test --manifest-path rust/Cargo.toml --quiet build_root_export_index_serializes_expected_shape`, `cargo test --manifest-path rust/Cargo.toml --quiet export_dashboards_with_request_all_orgs_aggregates_results`, `cargo test --manifest-path rust/Cargo.toml --quiet export_dashboards_with_dry_run_keeps_output_dir_empty`, `cargo test --manifest-path rust/Cargo.toml --quiet export_dashboards_writes_provisioning_artifacts_in_separate_lane`, `cargo test --manifest-path rust/Cargo.toml --quiet inspect_live_dashboards_with_request_all_orgs_matches_export_root_governance_contract`, `cargo fmt --manifest-path rust/Cargo.toml --all --check`
- Impact: `rust/src/commands/dashboard/cli_defs_command.rs`, `rust/src/commands/dashboard/export.rs`, `rust/src/commands/dashboard/files.rs`, `rust/src/commands/dashboard/models.rs`, `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/dashboard/inspect_live.rs`, `rust/src/commands/dashboard/dashboard_export_import_inventory_rust_tests.rs`, `rust/src/commands/dashboard/dashboard_export_import_topology_import_format_rust_tests.rs`, `rust/src/commands/dashboard/export_focus_report_path_top_rust_tests.rs`, `rust/src/commands/dashboard/inspect_live_export_parity_all_orgs_rust_tests.rs`, `README.md`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: medium-low. The new lane is additive, but any future work should keep `raw/` reserved for the grafana-util replay contract and avoid letting provisioning-specific semantics leak into existing import/diff code paths. The generated provider YAML intentionally carries a placeholder path, so operators still need to set the real Grafana-visible dashboard path before use.
- Follow-up: add dedicated provisioning export content tests that assert the provider YAML and provisioning dashboard tree for both single-org and all-org exports, then decide whether datasource provisioning should follow the same separate-lane pattern.

## 2026-03-31 - Add datasource provisioning export lane
- Summary: added a dedicated datasource `provisioning/` export lane instead of overloading the existing `datasources.json` contract. Datasource export now writes `provisioning/datasources.yaml` by default, records the provisioning path in export index/metadata, and writes one root provisioning file for all-org exports while leaving import/diff rooted on `datasources.json`.
- Tests: added CLI parsing coverage for `--without-datasource-provisioning` and helper-level shape coverage for the datasource provisioning document plus provisioning variant pointers in the export index.
- Test Run: `cargo test --manifest-path rust/Cargo.toml parse_datasource_export_supports_without_provisioning_flag`, `cargo test --manifest-path rust/Cargo.toml build_datasource_provisioning_document_projects_expected_shape`, `cargo test --manifest-path rust/Cargo.toml build_export_index_includes_provisioning_variant_pointer`
- Impact: `rust/src/commands/datasource/cli/defs.rs`, `rust/src/commands/datasource/mod.rs`, `rust/src/commands/datasource/export/support.rs`, `rust/src/commands/datasource/import_export.rs`, `rust/src/commands/datasource/tests/mod.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: medium-low. The lane is additive, but future datasource work should keep provisioning YAML deliberately separate from the stricter import/diff contract so the JSON replay surface does not become ambiguous. The current provisioning export uses only the normalized fields already captured by datasource export, so richer datasource plugin-specific provisioning knobs remain a later extension point.
- Follow-up: add an end-to-end export test that asserts the written `provisioning/datasources.yaml` file contents for single-org and all-org exports, and decide whether the docs should start recommending the provisioning lane explicitly for file-managed Grafana installs.

## 2026-03-31 - Add provisioning import formats for dashboards and datasources
- Summary: made the new provisioning lanes round-trippable by adding explicit import input formats on both surfaces. `dashboard import --input-format provisioning` now resolves either the provisioning root or its `dashboards/` subdirectory to the actual dashboard JSON tree, and `datasource import --input-format provisioning` now loads normalized datasource import records directly from Grafana provisioning YAML while leaving the old inventory contract untouched.
- Tests: dashboard coverage now includes parser/help/import-path regressions for the provisioning format and its interactive path resolution; datasource coverage now includes parser support for the new input format plus provisioning YAML loader/normalization tests.
- Test Run: `cargo test --manifest-path rust/Cargo.toml dashboard`, `cargo test --manifest-path rust/Cargo.toml datasource`, plus focused datasource tests `cargo test --manifest-path rust/Cargo.toml parse_datasource_export_supports_without_provisioning_flag`, `cargo test --manifest-path rust/Cargo.toml build_datasource_provisioning_document_projects_expected_shape`, `cargo test --manifest-path rust/Cargo.toml build_export_index_includes_provisioning_variant_pointer`
- Impact: `rust/src/commands/dashboard/cli_defs_command.rs`, `rust/src/commands/dashboard/files.rs`, `rust/src/commands/dashboard/import.rs`, `rust/src/commands/dashboard/import_apply.rs`, `rust/src/commands/dashboard/import_validation.rs`, `rust/src/commands/dashboard/import_interactive_loader.rs`, `rust/src/commands/dashboard/import_interactive_review.rs`, `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/dashboard/*_rust_tests.rs`, `rust/src/commands/datasource/cli/defs.rs`, `rust/src/commands/datasource/mod.rs`, `rust/src/commands/datasource/import_export.rs`, `rust/src/commands/datasource/import/support.rs`, `rust/src/commands/datasource/import/routed.rs`, `rust/src/commands/datasource/tests/mod.rs`, `rust/src/commands/datasource/tests/tail.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: medium. The behavior is still explicit and opt-in via `--input-format`, but future work should avoid teaching either import path to auto-detect formats by guesswork; the explicit lane split is the thing keeping the round-trip semantics understandable.
- Follow-up: add end-to-end import fixtures for real provisioning exports on both domains, then decide whether bundle/change surfaces should learn about provisioning inputs directly or keep consuming only the existing replay contracts.

## 2026-03-31 - Extend provisioning lanes into inspect and staged bundle/status surfaces
- Summary: carried the explicit provisioning split past import/export and into the next operator-facing surfaces. `dashboard inspect-export` now accepts `--input-format raw|provisioning` so offline analysis can target the provisioning dashboard tree directly, and staged datasource consumers now have explicit provisioning inputs through `change bundle --datasource-provisioning-file`, `overview --datasource-provisioning-file`, and `status staged --datasource-provisioning-file`.
- Tests: added parser/help/path-resolution coverage for `dashboard inspect-export` provisioning mode, plus focused bundle/overview/project-status tests for the new datasource provisioning input and its metadata propagation. Also tightened the staged CLI surface so provisioning and inventory datasource inputs conflict instead of silently competing.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_inspect_export_provisioning_input_format`, `cargo test --manifest-path rust/Cargo.toml --quiet inspect_export_help_lists_datasource_uid_report_column`, `cargo test --manifest-path rust/Cargo.toml --quiet analyze_export_dir_supports_explicit_provisioning_input_format`, `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_datasource_provisioning_section`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_args_support_datasource_provisioning_file`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_help_and_parse_support_datasource_provisioning_file`, `cargo test --manifest-path rust/Cargo.toml --quiet parse_change_cli_supports_bundle_provisioning_file`, `cargo test --manifest-path rust/Cargo.toml --quiet run_sync_cli_bundle_preserves_datasource_metadata_from_provisioning_file`
- Impact: `rust/src/commands/dashboard/cli_defs_inspect.rs`, `rust/src/commands/dashboard/inspect.rs`, `rust/src/commands/dashboard/inspect_orchestration.rs`, `rust/src/commands/dashboard/help.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/commands/dashboard/*inspect*_rust_tests.rs`, `rust/src/commands/sync/bundle_inputs.rs`, `rust/src/commands/sync/mod.rs`, `rust/src/commands/sync/cli.rs`, `rust/src/commands/sync/*bundle*_rust_tests.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/artifacts.rs`, `rust/src/commands/status/overview/tests.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/tests.rs`, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: medium-low. The changes stay explicit and additive, but future staged workflow work should preserve the current lane discipline: raw/inventory stays the default replay contract, provisioning stays opt-in and must not be reintroduced through format guessing.
- Follow-up: decide whether staged dashboard consumers beyond `inspect-export` should ever accept provisioning as a first-class source, or whether dashboard staged workflows should remain intentionally tied to the richer raw replay lane.

## 2026-03-30 - Extract shared status live support helpers
- Summary: finished the next `status` ownership cleanup by moving Grafana auth/header and client construction out of `project_status_command.rs` into a dedicated `project_status_support.rs` module. The shared live runtime now consumes those helpers directly, and the command module is down to command-surface concerns: args, dispatch, and shared rendering.
- Tests: kept the `X-Grafana-Org-Id` header regression with the new support module and revalidated the focused live-runtime tests after the extraction.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet resolve_live_project_status_headers_adds_org_scope_when_requested -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_dashboard_status_uses_dashboard_version_history_for_freshness -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_multi_org_domain_status_with_orgs -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_sync_status_uses_staged_input_metadata_for_freshness -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_promotion_status_uses_staged_input_metadata_for_freshness -- --nocapture`
- Impact: `rust/src/lib.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/live.rs`, `rust/src/commands/status/support.rs`, `docs/DEVELOPER.md`, `docs/internal/project-surface-boundaries.md`, `docs/internal/project-status-architecture.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The extraction is behavior-preserving, but future `status live` changes should keep reusable Grafana client/header code in `project_status_support.rs` rather than drifting back into the command module or into domain producers.
- Follow-up: if the `status` line keeps growing, the next cleanup target should be deciding whether the shared text rendering also belongs in a thinner presentation-only module or remains small enough to stay in the command surface.

## 2026-03-30 - Extract shared live project-status runtime
- Summary: completed the second ownership refactor behind the public `status` surface by moving shared live status assembly out of `rust/src/commands/status/mod.rs`. The new `rust/src/commands/status/live.rs` now owns the multi-org fan-out path, per-domain live producer wiring, live freshness sampling, staged sync/promotion metadata overlays, and final `ProjectStatus` assembly, while `project_status_command.rs` stays focused on CLI args, shared rendering, dispatch, and Grafana client/header setup.
- Tests: revalidated the moved live-runtime unit coverage for dashboard version-history freshness, multi-org aggregation, staged sync/promotion freshness overlays, timestamp sampling, and shared text rendering. Added focused renderer regressions for omitting empty blocker/action sections and capping top blocker/action output to five rows.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet project_status_live_text_renderer_surfaces_overall_domain_and_action_sections -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_live_text_renderer_skips_empty_blocker_and_action_sections -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_live_text_renderer_limits_top_sections_to_five_items -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_dashboard_status_uses_dashboard_version_history_for_freshness -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_multi_org_domain_status_with_orgs -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_sync_status_uses_staged_input_metadata_for_freshness -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_live_promotion_status_uses_staged_input_metadata_for_freshness -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_freshness_samples_from_value_uses_timestamp_fields_from_arrays_and_objects -- --nocapture`
- Impact: `rust/src/lib.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/live.rs`, `rust/src/commands/status/tests.rs`, `docs/DEVELOPER.md`, `docs/internal/project-surface-boundaries.md`, `docs/internal/project-status-architecture.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. The contract shape is intentionally unchanged, but live status assembly now crosses one more internal module boundary, so future changes should add live-domain logic in `project_status_live_runtime.rs` or the owning domain producer instead of drifting back into the command facade.
- Follow-up: the next cleanup target should be narrowing `project_status_command.rs` even further to command-surface concerns only, for example by deciding whether the shared Grafana client/header helpers should remain there or move into a dedicated status-support module.

## 2026-03-30 - Rename project surfaces to `overview` / `status` / `change` and remove web
- Summary: completed the one-shot high-level surface rename so the active operator vocabulary is now `overview`, `status`, and `change` with no compatibility alias layer. The Rust CLI parser/help surfaces, focused tests, README, and both user guides all now use the new names. In the same pass, removed the unused local `grafana-util-web` workbench entirely, including the Cargo `web` feature, optional web-only dependencies, the separate binary target, and the whole `rust/src/web/` tree.
- Tests: revalidated focused Rust command/help coverage after the rename, rechecked overview/status command behavior, and reran the Rust library suite after removing the web feature and module tree.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet cli_rust_tests -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet sync::cli_help_rust_tests -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet sync::cli_apply_review -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_rust_tests -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet --lib`, `rg -n "grafana-util sync|project-status" rust/src README.md README.zh-TW.md docs/user-guide.md docs/user-guide-TW.md`, `git diff --check -- README.md README.zh-TW.md docs/user-guide.md docs/user-guide-TW.md rust/Cargo.toml rust/Cargo.lock rust/src/cli/mod.rs rust/src/cli/help_examples.rs rust/src/cli/rust_tests.rs rust/src/lib.rs rust/src/commands/status/overview/mod.rs rust/src/commands/status/overview/tests.rs rust/src/commands/status/contract.rs rust/src/commands/status/mod.rs rust/src/commands/sync/mod.rs rust/src/commands/sync/cli_help_rust_tests.rs docs/internal/ai-status.md docs/internal/ai-changes.md`
- Impact: `README.md`, `README.zh-TW.md`, `docs/user-guide.md`, `docs/user-guide-TW.md`, `rust/Cargo.toml`, `rust/Cargo.lock`, `rust/src/cli/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/lib.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/tests.rs`, `rust/src/commands/status/contract.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/sync/mod.rs`, `rust/src/commands/sync/cli_help_rust_tests.rs`, `rust/src/bin/grafana-util-web.rs`, `rust/src/web/*`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The rename is intentionally surface-only for command vocabulary and does not try to rename internal `sync` JSON kinds or builder symbols, but it is still a top-level CLI/docs change that can break external wrapper scripts if they depended on the removed names. Removing the web workbench lowers maintenance cost, but it also drops any undocumented local workflows that had started to rely on the old Axum shell.
- Follow-up: the next architectural pass should keep tightening the ownership boundary behind the new names: `overview` stays the human entrypoint, `status` stays the canonical readiness contract, and `change` stays the review/apply workflow.

## 2026-03-30 - Add current project-surface boundary docs
- Summary: started the first post-rename cleanup pass for maintainer docs instead of changing runtime semantics. Added a new current boundary note that defines the public `overview` / `status` / `change` model, the public-vs-internal naming split, and the current-vs-target ownership map for staged status assembly. Tightened `docs/DEVELOPER.md` with a public surface map, explicit naming policy, and a bounded domain freeze policy, then updated the internal index and architecture notes so `project-status` and `sync` are clearly described as internal architecture/runtime terms rather than current public commands.
- Tests: doc-only. Revalidated by targeted text inspection and `git diff --check`.
- Impact: `docs/DEVELOPER.md`, `docs/internal/README.md`, `docs/internal/overview-architecture.md`, `docs/internal/project-status-architecture.md`, `docs/internal/project-surface-boundaries.md`, `README.zh-TW.md`, `docs/user-guide-TW.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This pass changes maintainer guidance and a few public wording clarifications only, but future runtime work now has a documented target and should follow the new naming bridge instead of reintroducing public/internal drift.
- Follow-up: the next runtime slice should move staged `status` ownership out of `overview` and then update the architecture notes from current-vs-target wording to the new actual call path.

## 2026-03-30 - Extract shared staged project-status builder
- Summary: completed the first runtime ownership refactor behind the new `overview` / `status` split. Added a shared staged project-status builder module, moved the cross-domain staged status assembly out of `overview_document.rs`, and rewired `execute_project_status_staged()` so it no longer builds a full `OverviewDocument` just to read `project_status`. `overview` now consumes that same shared staged builder when assembling its document, which keeps the public overview payload stable while aligning ownership with the intended architecture.
- Tests: `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_staged_ -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_preserves_the_shared_project_status_render_contract -- --nocapture`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_live_text_renderer_surfaces_overall_domain_and_action_sections -- --nocapture`
- Impact: `rust/src/lib.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/tests.rs`, `rust/src/commands/status/staged.rs`, `docs/internal/project-surface-boundaries.md`, `docs/internal/overview-architecture.md`, `docs/internal/project-status-architecture.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. This changes staged ownership only and preserves the shared status contract shape, but future staged signal work should now land in the shared staged builder instead of drifting back into `overview_document.rs`.
- Follow-up: the next cleanup target should be shrinking `project_status_command.rs` by extracting more live-status assembly out of the command module.

## 2026-03-30 - Alert authoring round-trip normalization and live smoke
- Summary: closed the known Alert V2 authoring round-trip drift inside the compare path instead of changing apply semantics. Alert compare payloads are now normalized across desired and live reads so authoring-created contact points, managed notification policies, and simple rules do not come back as false-positive updates just because Grafana injected default fields, omitted empty objects, reordered matchers, or serialized integral floats differently. The Docker live smoke now also includes a real authoring path from scaffold to apply to prune delete.
- Tests: added focused Rust regressions for compare normalization and for post-apply noop planning, then revalidated the alert live smoke end to end.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet alert -- --test-threads=1` passed; `RUST_LIVE_SCOPE=alert bash scripts/test-rust-live-grafana.sh` passed against Grafana `12.4.1`; `git diff --check -- rust/src/commands/alert/support/mod.rs rust/src/commands/alert/runtime_support.rs rust/src/commands/alert/import_diff.rs rust/src/commands/alert/mod.rs rust/src/commands/alert/tests/mod.rs scripts/test-rust-live-grafana.sh docs/internal/ai-status.md docs/internal/ai-changes.md` passed.
- Impact: `rust/src/commands/alert/mod.rs`, `rust/src/commands/alert/support/mod.rs`, `rust/src/commands/alert/runtime_support.rs`, `rust/src/commands/alert/import_diff.rs`, `rust/src/commands/alert/tests/mod.rs`, `scripts/test-rust-live-grafana.sh`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The runtime change is intentionally compare-only and does not widen live mutation semantics, but future authoring work still needs to keep desired and live normalization scoped to known Grafana defaults rather than masking substantive config drift.
- Follow-up: if more authoring surfaces are added, extend the same normalization discipline and smoke coverage before documenting them as round-trip clean.

## 2026-03-30 - Alert plan/apply/delete management lane
- Summary: added a new operator-first alert management lane under `grafana-util alert` instead of forcing alert mutations through the older export/import workflow. The alert CLI now exposes `plan`, `apply`, `delete`, `init`, `new-rule`, `new-contact-point`, and `new-template`, while the runtime can build reviewable plan documents from desired YAML or JSON files, execute reviewed create/update/delete operations against Grafana provisioning APIs, preview explicit deletes with policy-reset guarding, and initialize or scaffold a managed alert desired-state tree.
- Tests: added parser/help coverage for the new alert commands plus focused alert runtime coverage for JSON+YAML desired loading, plan row generation, prune-gated deletes, plan execution, policy delete protection, and scaffold/init output.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo fmt --manifest-path rust/Cargo.toml --all --check`, `cargo test --manifest-path rust/Cargo.toml --quiet alert -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet`
- Impact: `rust/src/commands/alert/mod.rs`, `rust/src/commands/alert/cli/mod.rs`, `rust/src/commands/alert/import_diff.rs`, `rust/src/commands/alert/runtime_support.rs`, `rust/src/commands/alert/support/mod.rs`, `rust/src/commands/alert/tests/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The new alert lane intentionally reuses the existing alert document shape and provisioning request mapping, which keeps compatibility with export/import artifacts, but it also introduces a second mutation-oriented surface under `alert`, so future work should keep the distinction clear: `import/diff` remains migration/inventory oriented, while `plan/apply/delete` is the review-first operator lane. Multi-org live mutation is intentionally still out of scope for this pass.
- Follow-up: the next bounded pass should improve operator ergonomics rather than widen resource scope again, for example by adding richer text rendering for plan/apply output, optional reviewed-plan metadata, or scaffold coverage for mute timings and policy trees.

## 2026-03-30 - Alert V2 public docs and operator examples
- Summary: refreshed the public alert docs to match the current Alert V2 workflow instead of the older scaffold-first wording. README and user-guide content now split alert work into authoring, review/apply, and migration layers; document the new `add-rule`, `clone-rule`, `add-contact-point`, `set-route`, and `preview-route` surfaces; and call out the operator boundaries that matter, including desired-only authoring, managed-route overwrite semantics, `preview-route` not being a full routing simulator, and `--folder` not being a live folder creation workflow.
- Tests: ran live documentation validation against a temporary Docker Grafana `12.4.1` instance for `alert plan`, `alert apply`, and `alert plan --prune` / `alert apply` delete; also rechecked the new authoring command help and output shapes locally. No runtime code changes were made in this doc pass.
- Test Run: `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert --help`, `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert add-rule --help`, `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert clone-rule --help`, `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert add-contact-point --help`, `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert set-route --help`, `cargo run --manifest-path rust/Cargo.toml --quiet --bin grafana-util -- alert preview-route --help`, `docker run -d --name codex-alert-docs-43111 -p 43111:3000 grafana/grafana:12.4.1`, live `curl` checks against `http://127.0.0.1:43111`, and live `grafana-util alert plan/apply` plus prune-delete reruns using the temporary fixture under `test-results/alert-authoring-docs-live-host`
- Impact: `README.md`, `README.zh-TW.md`, `docs/user-guide.md`, `docs/user-guide-TW.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This pass is documentation-only, but it records one current runtime limitation that operators need to know: the validated authoring/apply flow still replans as `update` on some resources after apply because Grafana normalizes parts of the live payload shape, so the current authoring lane is not yet a strict all-`noop` round-trip after one apply.
- Follow-up: if maintainers want the docs to claim a clean round-trip, the runtime needs a separate normalization/parity pass for authored contact-point, policy, and rule documents after live apply. That work should stay explicitly separate from this documentation refresh.

## 2026-03-30 - Wire all-orgs live project-status execution
- Summary: turned `project-status live` org scoping from a help-only contract into real runtime behavior. The shared live path now builds org-scoped clients with `X-Grafana-Org-Id`, uses `/api/orgs` to enumerate visible orgs for `--all-orgs`, and aggregates live dashboard, datasource, alert, and access domain status across orgs instead of silently reporting only the default org. `overview live` continues to stay thin and now inherits the same real scoped live execution path.
- Tests: added focused CLI parser/help regressions for `project-status live` and `overview live`, plus shared live-path tests that assert explicit `--org-id` scoping and `--all-orgs` fan-out hit the expected Grafana APIs with per-org headers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo fmt --manifest-path rust/Cargo.toml --all --check`, `cargo test --manifest-path rust/Cargo.toml project_status_live -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml overview_live -- --test-threads=1`, `cargo test --manifest-path rust/Cargo.toml --quiet`
- Impact: `rust/src/commands/status/mod.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The change now depends on `/api/orgs` being readable before multi-org aggregation can fan out, so environments with restricted org-list permissions still degrade to the existing read-failed domain status instead of producing a silent partial snapshot. Future all-orgs work should reuse the same org-scoped client builder rather than duplicating header/fan-out logic elsewhere.
- Follow-up: the remaining inventory gap backlog still applies for other domains such as `alert export/diff`, `access team`, and `access service-account`; they should reuse the same org enumeration and scoped-client pattern instead of inventing separate multi-org semantics.

## 2026-03-30 - Connection-first web flow and `web/mod.rs` split
- Summary: reworked the Rust web shell so connection setup is an explicit first step instead of an implicit shared form. The page now exposes a dedicated `Test Connection` action with status feedback, keeps the connection note/status visible ahead of workspace execution, and makes the task flow read as connect -> choose workspace/action -> review output. Internally, the old oversized `rust/src/web/mod.rs` was split by concern into contracts, registry, page, and handlers modules while `mod.rs` now stays focused on router/server wiring plus tests.
- Tests: added a focused connection-probe unit test over the new shared handler helper, kept the existing web registry/browse/overview/sync tests, and re-ran the earlier dashboard inspect focused regressions after the refactor.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`, `node --check rust/src/web/index.js`, `cargo test --manifest-path rust/Cargo.toml --features web --quiet web::tests::`, `cargo test --manifest-path rust/Cargo.toml --quiet parse_inspect_vars_args_supports_output_file`, `cargo test --manifest-path rust/Cargo.toml --quiet render_export_inspection_report_output_renders_dependency_text_and_json_distinctly`
- Impact: `rust/src/web/mod.rs`, `rust/src/web/handlers.rs`, `rust/src/web/contracts.rs`, `rust/src/web/registry.rs`, `rust/src/web/page.rs`, `rust/src/web/index.js`, `rust/src/web/index.css`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. The flow change is UI-only on top of existing shared execution seams, but the new module split means future web work should keep request/contract types, page markup, registry metadata, and execution handlers separated instead of drifting back into one root file.
- Follow-up: keep extending the same connection-first shell for datasource, alert, access, and future mutation previews, and split `handlers.rs` further if it starts accumulating unrelated domain logic the same way `mod.rs` previously did.

## 2026-03-30 - Dashboard-first web workspace over shared Rust execution seams
- Summary: replaced the initial raw JSON capability console in `grafana-util-web` with a dashboard-first workspace shell. The new web layer now serves workspace/action metadata, keeps connection settings in browser session storage, and exposes dashboard `browse`, `list`, `inspect-live`, `inspect-export`, and `inspect-vars` through shared Rust execution helpers instead of stdout-oriented command wrappers. The backend also now builds a dedicated dashboard browse document with tree/list/detail payloads so the browser UI can stay presentation-only.
- Tests: added focused web tests for the workspace registry and dashboard browse document builder, and revalidated the existing staged overview and sync-summary web handler coverage. Also re-ran focused non-web dashboard tests around inspect-vars parsing and dependency report rendering after the shared inspect seam changes.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo test --manifest-path rust/Cargo.toml --features web --quiet web::tests::`, `node --check rust/src/web/index.js`, `cargo test --manifest-path rust/Cargo.toml --quiet parse_inspect_vars_args_supports_output_file`, `cargo test --manifest-path rust/Cargo.toml --quiet render_export_inspection_report_output_renders_dependency_text_and_json_distinctly`
- Impact: `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/dashboard/inspect.rs`, `rust/src/commands/dashboard/inspect_live.rs`, `rust/src/commands/dashboard/inspect_output.rs`, `rust/src/commands/dashboard/list.rs`, `rust/src/commands/dashboard/vars.rs`, `rust/src/web/mod.rs`, `rust/src/web/index.js`, `rust/src/web/index.css`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The web shell now depends on a larger dashboard execution surface and more frontend state logic, so future work should keep new domain actions following the same pattern: add or reuse Rust execution/document seams first, then register them in the workspace shell. Browse remains intentionally web-specific in presentation, so any future contract changes should preserve the current tree/list/detail backend shape or migrate the frontend in the same change.
- Follow-up: extend the same workspace/action contract to datasource, alert, access, and the remaining dashboard mutation/report flows, while keeping confirmable live mutations review-first instead of direct form submission.

## 2026-03-30 - Local-only `grafana-util-web` workbench over shared execution contracts
- Summary: added the first Rust local web workbench as a separate `grafana-util-web` binary behind a new Cargo `web` feature. The change keeps the default CLI build free of web dependencies, exposes reusable execution seams for `overview`, `project-status`, and selected read-only `sync` commands, and adds a thin Axum page/API layer that binds to localhost by default, keeps credentials request-scoped and in-memory, and renders shared text/JSON documents without introducing a frontend build toolchain or any live-apply path.
- Tests: added focused web router/handler tests for the capability registry, sync summary execution, and staged overview execution; also revalidated that the default non-web build still runs existing Rust tests.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`, `cargo test --manifest-path rust/Cargo.toml --features web --quiet web::tests::`, `cargo test --manifest-path rust/Cargo.toml --quiet run_sync_cli_summary_accepts_local_desired_file`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_live_alias_dispatches_to_project_status_live_runner`
- Impact: `rust/Cargo.toml`, `rust/src/lib.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/sync/mod.rs`, `rust/src/commands/sync/cli.rs`, `rust/src/web/mod.rs`, `rust/src/web/index.css`, `rust/src/web/index.js`, `rust/src/bin/grafana-util-web.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The web workbench is intentionally thin and local-only, but it introduces new optional build surface plus shared execution seams, so future work needs to keep render-only concerns in the web layer and avoid backsliding into web-only business logic.
- Follow-up: if new web capabilities are added, prefer extending the shared execution/document builders first and only then registering them in the web capability registry; keep live mutation out of the web surface until there is a reviewed confirmation path built on the same shared contracts.

## 2026-03-30 - `overview live` convenience entry over canonical live status
- Summary: added `grafana-util overview live ...` as a thin convenience entrypoint that delegates to the existing `project-status live` path instead of introducing new live aggregation logic under `overview`. Staged `overview` behavior remains unchanged, while help text and maintainer docs now describe the staged/live split more explicitly.
- Tests: updated focused overview parser/help and shared-contract tests to cover the new CLI shape and preserve canonical project-status rendering semantics.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_overview_live_command`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_args_parse_and_help_expose_output_mode`, `cargo test --manifest-path rust/Cargo.toml --quiet overview_live_alias_dispatches_to_project_status_live_runner`, `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_preserves_the_shared_project_status_render_contract`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_live_document_serializes_the_shared_contract_shape`, `cargo test --manifest-path rust/Cargo.toml --quiet dispatch_routes_overview_group_to_overview_handler`
- Impact: `rust/src/commands/status/overview/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/overview-architecture.md`, `docs/internal/current-capability-inventory-2026-03-30.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is deliberately thin and routes through the existing canonical live status surface, but any future work must keep live derivation out of `overview_artifacts.rs` and `overview_document.rs`.
- Follow-up: if operator feedback wants richer live browsing, extend the shared `project-status` interactive surface first and let `overview live` inherit that behavior instead of inventing a second live workbench.

## 2026-03-30 - Faster pre-workbench query analysis for `dashboard inspect-export --interactive`
- Summary: reduced the apparent hang before the inspect workbench launches on large dashboard export trees by removing repeated regex compilation from the hottest query-analysis helpers. The interactive path already showed progress messages; this pass makes the expensive `building query report` stage materially cheaper without changing inspect semantics or the resulting report rows.
- Tests: reused the existing focused inspect workbench document/render coverage after the extraction changes.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`, `cargo test --manifest-path rust/Cargo.toml --quiet build_inspect_workbench_document_adds_dependency_coverage_views`, `cargo test --manifest-path rust/Cargo.toml --quiet inspect_workbench_render_surfaces_header_panes_and_footer`
- Impact: `rust/src/commands/dashboard/inspect_query_report.rs`, `rust/src/commands/dashboard/inspect_extract_query.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to medium. The logic is intended to be behavior-preserving, but it touches shared query extraction helpers across Prometheus, Flux, InfluxQL, and SQL paths, so future regressions should be checked first in inspect/query-report focused tests.
- Follow-up: if very large exports are still slow after this, profile the remaining pre-workbench builders before adding any more interactivity-specific workarounds.

## 2026-03-30 - Tool-version metadata across staged Rust outputs
- Summary: added a consistent additive `toolVersion` field to the main Rust staged/export/status documents so future compatibility code can tell which `grafana-util` version produced an artifact without overloading `schemaVersion`. The version is sourced from the Rust crate version through a shared helper, and readers continue to accept older artifacts that do not have the new field.
- Tests: updated focused contract tests for `overview`, `project-status`, alert export/root-index, sync summary, source-bundle, bundle-preflight, and promotion-preflight.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`, `cargo test --manifest-path rust/Cargo.toml --quiet project_status_document_serializes_the_shared_contract_shape`, `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_all_sections`, `cargo test --manifest-path rust/Cargo.toml --quiet build_empty_root_index_contains_version_markers`
- Impact: `rust/src/common/mod.rs`, `rust/src/commands/dashboard/models.rs`, `rust/src/commands/dashboard/files.rs`, `rust/src/commands/datasource/export/support.rs`, `rust/src/commands/alert/support/mod.rs`, `rust/src/commands/sync/summary_builder.rs`, `rust/src/commands/sync/bundle_builder.rs`, `rust/src/commands/sync/root_preflight/mod.rs`, `rust/src/commands/sync/preflight.rs`, `rust/src/commands/sync/plan_builder.rs`, `rust/src/commands/sync/apply_builder.rs`, `rust/src/commands/sync/audit.rs`, `rust/src/commands/sync/promotion_preflight.rs`, `rust/src/commands/alert/sync.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/contract.rs`
- Rollback/Risk: low. This is additive metadata only, but downstream consumers that snapshot exact full JSON documents may need fixture updates when they start asserting the new field.
- Follow-up: when a future compatibility decision needs to branch by producer behavior, prefer `schemaVersion` first and `toolVersion` second.

## 2026-03-30 - Shared TUI presentation pass for overview, project-status, dashboard, and datasource
- Summary: tightened TUI presentation consistency without changing any status or workflow contracts. The shared shell now provides aligned summary cells and footer control cells, `overview` no longer shows a duplicated top summary block, and the main project/browser surfaces use the same brighter label/accent hierarchy so key state is easier to scan.
- Tests: updated the focused dashboard and datasource browse-header tests to assert rendered `Line` text after the summary rows became styled line buffers instead of plain strings.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`, `cargo test --manifest-path rust/Cargo.toml --quiet project_home_lines_surface_status_and_next_action`, `cargo test --manifest-path rust/Cargo.toml --quiet summary_lines_surface_focus_and_mode`, `cargo test --manifest-path rust/Cargo.toml --quiet summary_lines_surface_pending_delete_mode`, `cargo test --manifest-path rust/Cargo.toml --quiet summary_lines_move_status_out_of_the_header`
- Impact: `rust/src/common/tui/shell.rs`, `rust/src/commands/status/overview/tui.rs`, `rust/src/commands/status/overview/tui_render.rs`, `rust/src/commands/status/tui/render.rs`, `rust/src/commands/dashboard/browse_render.rs`, `rust/src/commands/datasource/browse/render.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is presentation-only and preserves current TUI ownership boundaries, but any tests that assumed plain-string summary rows needed to move to `Line::to_string()` assertions.
- Follow-up: if more interactive surfaces need the same treatment, prefer extending the shared shell helpers rather than reintroducing one-off spacing rules per TUI.

## 2026-03-30 - Overview compatibility for legacy dashboard export indexes
- Summary: relaxed the dashboard variant-index contract just enough to accept older staged exports whose `index.json` rows do not carry `org` or `orgId`. The existing dashboard inspection summary path now treats those fields as optional and falls back to unknown export identity instead of rejecting the entire raw export root.
- Tests: added a focused regression for a legacy raw export directory whose `index.json` omits org identity fields.
- Test Run: pending local rerun.
- Impact: `rust/src/commands/dashboard/models.rs`, `rust/src/commands/dashboard/export_focus_report_query_presentation_summary_inventory_rust_tests.rs`
- Rollback/Risk: low. This is additive compatibility for older single-scope exports and preserves current behavior when org identity fields are present.
- Follow-up: none.

## 2026-03-30 - Project-status workbench clarity
- Summary: clarified the project-status TUI presentation without changing the shared status contract or handoff logic. The home pane now explains the project-home -> domain -> action path more directly, surfaces the current top blocker plus recommended domain/action using existing `ProjectStatus` data, and keeps the current behavior as a thin consumer of the document.
- Tests: no new tests added; existing Rust coverage was used to protect the presentation-only change.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet project_status_tui --test project_status_tui_rust_tests` passed.
- Impact: `rust/src/commands/status/tui/mod.rs`, `rust/src/commands/status/tui/render.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is copy-only work in the TUI layer, but the text still needs to preserve the current test-visible substrings if the existing Rust assertions remain unchanged.
- Follow-up: none.

## 2026-03-30 - Maintenance-mode and consumer-driven reopen guidance
- Summary: tightened the current maintainer docs so the repo no longer merely says "stop-for-now" per domain; it now also states the default operating mode after closeout. The capability inventory now explains maintenance mode and allowed near-term work, the execution review spells out the consumer-driven reopen rule, and the roadmap explicitly treats docs/help/TUI clarity as the default next work when no domain lane is justified.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/current-capability-inventory-2026-03-30.md`, `docs/internal/current-execution-review-2026-03-29.md`, `docs/internal/project-roadmap.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This only changes maintainer-facing guidance.
- Follow-up: use this as the default decision rule before reopening any domain-owned producer lane.

## 2026-03-29 - Sync staged provider and placeholder review evidence
- Summary: added a current capability inventory for maintainers so the project's matured operator surface can be read as one practical snapshot instead of being reconstructed from roadmap, execution-review, and architecture documents. The new note summarizes domain capabilities, project-level surfaces, and the current "do not expand by default" posture, and the maintainer doc routing now points to it from the Rust overview and internal docs index.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/current-capability-inventory-2026-03-30.md`, `docs/overview-rust.md`, `docs/internal/README.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This changes maintainer-facing documentation only.
- Follow-up: use this inventory as the top-level capability snapshot; keep roadmap and review notes focused on execution posture rather than repeating the same capability summary.

## 2026-03-29 - Sync staged provider and placeholder review evidence
- Summary: kept the next sync bounded pass inside `rust/src/commands/sync/project_status.rs` and preserved more staged trust-chain evidence without widening inputs. When bundle-preflight already carries provider or secret-placeholder assessment plans but neither assessment is blocking, the sync row now emits `provider-review` and `secret-placeholder-review` warnings instead of dropping that reviewable evidence.
- Tests: added focused sync producer coverage for non-blocking provider and secret-placeholder review evidence and revalidated the shared overview/project-status consumers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_sync_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/sync/project_status.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change stays document-driven and bounded to the sync-owned staged producer, but it does add more review-only evidence counts, so consumers should keep treating warning counts as traceable staged evidence rather than severity labels.
- Follow-up: if sync remains the only active lane after this, the next bounded pass should favor explicit plan/audit evidence over additional assessment-level review hints.

## 2026-03-29 - Project-level stop/continue decision refresh
- Summary: refreshed the maintainer-facing project review after the latest bounded producer passes. `dashboard`, `datasource`, `alert`, and `access` are now all explicitly stop-for-now unless a concrete consumer proves a missing decision-critical signal, while `sync` / `promotion` remains the only active bounded depth lane for this pass. The current execution review, maturity review, producer gap list, and next-phase execution note now all align to that same decision.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/current-execution-review-2026-03-29.md`, `docs/internal/domain-producer-maturity-review-2026-03-29.md`, `docs/internal/next-phase-execution-plan-2026-03-29.md`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This changes maintainer-facing execution guidance only.
- Follow-up: if sync/promotion reaches stop-if-good-enough too, re-run this same review before opening any new depth lane.

## 2026-03-29 - Sync/promotion trust-chain evidence follow-through
- Summary: kept the next sync/promotion bounded pass inside the owning staged producers. `sync/project_status.rs` now preserves alert-artifact presence as `alert-artifact-review` evidence when alert artifacts exist without blocked or plan-only findings, and `sync/project_status_promotion.rs` now keeps `continuationSummary.resolvedCount` as the actual warning count when it is the chosen apply-continuation evidence source.
- Tests: added focused sync producer coverage for non-blocking alert-artifact review evidence and updated promotion producer expectations for resolved continuation counts, then revalidated the shared overview/project-status consumers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_sync_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_promotion_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/sync/project_status.rs`, `rust/src/commands/sync/project_status_promotion.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change stays document-driven and inside the sync-owned staged producers, but it does make the trust-chain rows carry slightly richer evidence counts, so downstream consumers should continue to treat warning counts as source-attributable staged evidence rather than severity labels.
- Follow-up: if sync/promotion gets another bounded pass later, prefer real plan/audit inputs or explicit continuation inventories before adding more fallback heuristics.

## 2026-03-29 - Datasource staged import-blocker promotion
- Summary: kept the next datasource bounded pass inside `rust/src/commands/datasource/project_status/staged.rs` and promoted staged import-preview `wouldBlock` from a warning into a real datasource blocker. The staged datasource row now becomes `blocked` with `blocked-by-blockers` when the existing import preview already says the datasource change is blocked, while diff/drift, secret-reference, and the rest of the import-preview warning signals stay additive.
- Tests: updated focused same-file datasource producer expectations for blocked import-preview rows and revalidated the shared overview/project-status consumers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_datasource_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/datasource/project_status/staged.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The change is still document-driven and bounded to the datasource-owned staged producer, but it does strengthen status semantics from warning-only to blocker-backed for `wouldBlock`, so downstream consumers that treated datasource staged rows as always `ready` when data existed must respect the existing shared blocked state.
- Follow-up: if datasource gets one more bounded pass after this, prefer sharper secret-reference and mutation/import evidence over adding another summary surface.

## 2026-03-29 - Dashboard staged datasource-coverage readiness follow-through
- Summary: kept the next dashboard bounded pass inside `rust/src/commands/dashboard/project_status.rs` and tightened the staged detail-ready check for `dashboardDependencies` rows. A row now needs not just `file`, `panelIds`, and `queryFields`, but also non-empty `datasources` and `datasourceFamilies` coverage before it counts as import-ready at the richer detail level.
- Tests: added focused same-file dashboard producer coverage for missing datasource coverage and revalidated the shared overview/project-status consumers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_dashboard_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/dashboard/project_status.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and stays inside the dashboard-owned staged producer, but future dashboard follow-up should still prefer existing inspect/dependency contract fields over inventing another readiness layer.
- Follow-up: if dashboard gets one more bounded pass after this, prefer richer governance exemplars rather than more import-readiness heuristics.

## 2026-03-29 - Alert staged readiness closure and stop review
- Summary: kept the final alert bounded pass inside `rust/src/commands/alert/project_status/staged.rs` and reused only the stable staged alert export summary counts that already exist. The staged alert producer now emits conservative prerequisite warnings when alert rules exist without any contact points or notification policies, and the maintainer review/execution docs now treat alert as stop-for-now instead of another active depth lane.
- Tests: updated same-file alert producer coverage and revalidated the shared consumers that depend on the alert row.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_alert_project_status_domain` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/alert/project_status/staged.rs`, `docs/internal/domain-producer-maturity-review-2026-03-29.md`, `docs/internal/next-phase-execution-plan-2026-03-29.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and stays inside the alert-owned staged producer, but future alert work should still avoid inventing a second summary or pushing derivation back into shared consumers.
- Follow-up: treat alert as stop-for-now unless a concrete consumer later needs more than the existing staged/live producers already expose.

## 2026-03-29 - Sync/promotion staged trust-chain evidence follow-through
- Summary: kept the next bounded pass inside the sync-owned staged producers instead of moving more heuristics back into `overview` or `project-status`. `sync/project_status.rs` now falls back to `secretPlaceholderAssessment.summary.blockingCount` when the top-level placeholder blocker count is absent, and `sync/project_status_promotion.rs` now accepts `handoffSummary.nextStage` and `continuationSummary.nextStage` as valid review/apply evidence when explicit instruction text is missing.
- Tests: revalidated the focused sync/promotion producer tests plus the shared overview and project-status consumers.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_sync_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_promotion_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests` passed.
- Impact: `rust/src/commands/sync/project_status.rs`, `rust/src/commands/sync/project_status_promotion.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and stays inside the sync-owned staged producers, but future work should still prefer existing staged review/apply documents over inventing another trust-summary layer.
- Follow-up: if sync/promotion gets one more bounded pass later, prefer real `plan` / `audit` evidence before adding any more fallback heuristics.

## 2026-03-29 - Bounded producer follow-through note
- Summary: refreshed the current-only maintainer docs so dashboard, datasource, sync/promotion, and alert are described as bounded mainline follow-through inside the owning modules instead of as open next-step targets or a new architecture branch. The maturity review now makes the stop/one-more-round split explicit, the gap list stays execution-oriented, and the next-phase note reads as current arrangement text instead of a slice queue.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/domain-producer-maturity-review-2026-03-29.md`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/next-phase-execution-plan-2026-03-29.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This only changes maintainer-facing wording.
- Follow-up: none.

## 2026-03-29 - Domain producer maturity review
- Summary: added one maintainer-facing maturity review that decides which domain-owned producers are already good enough to stop deepening, which still justify one more bounded round, and what practical role `overview` and `project-status` should keep. The review keeps dashboard, datasource, alert, sync, and promotion in the bounded follow-through set, marks access as good enough to stop for now, and explicitly narrows consumer-level work to presentation-only improvements.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/domain-producer-maturity-review-2026-03-29.md`, `docs/overview-rust.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This only changes maintainer-facing guidance.
- Follow-up: use this note with the gap list and execution plan before opening another bounded producer-deepening pass.

## 2026-03-29 - Dashboard staged import-readiness detail gaps
- Summary: deepened the dashboard-owned staged status producer by adding one more bounded import-readiness evidence layer. `dashboard/project_status.rs` still keeps the existing summary-backed `import-readiness-gaps` warning, but now also emits a stricter detail-backed warning when a `dashboardDependencies` row has a file yet still lacks the richer structural evidence needed to look import-ready.
- Tests: added focused same-file dashboard producer coverage and revalidated the shared consumers that depend on the dashboard row.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_dashboard_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_command --lib` passed.
- Impact: `rust/src/commands/dashboard/project_status.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is additive and stays inside the dashboard-owned producer, but future dashboard work should keep using existing inspect/dependency documents instead of inventing another readiness layer.
- Follow-up: if dashboard gets one more bounded pass later, prefer richer governance exemplars over new import heuristics.

## 2026-03-29 - Domain live-status depth pass
- Summary: pushed the next live-status depth pass down into domain-owned producers instead of growing more command-layer heuristics, keeping the change bounded to existing live/readiness surfaces. `dashboard/live_project_status.rs` now emits conservative folder-spread and root-scope warnings plus a bounded live import/dry-run readiness follow-up; `datasource_live_project_status.rs` adds missing name/access and org-scope drift checks plus live provider/placeholder readiness; `alert_live_project_status.rs` keeps a bounded live follow-up for import, diff, and promotion readiness behind rule linkage; `access/live_project_status.rs` now separates import-review findings from drift-severity findings; `sync/live_project_status_sync.rs` can interpret generic bundle `blockedCount` and `planOnlyCount`; `sync/live_project_status_promotion.rs` carries staged handoff/apply-continuation evidence; `project_status_freshness.rs` adds a timestamp-aware sample helper; and the dedicated `project-status` TUI now spells out the recommended handoff domain and action more explicitly.
- Tests: revalidated focused domain live tests plus shared consumers so the deeper live heuristics still fit the original product model of inventory/live inspection, output for later analysis, and reviewable export/import/sync workflows.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet build_access_live_domain_status_reports` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_datasource_live_project_status --lib` passed; `cargo test --manifest-path rust/Cargo.toml --quiet --test alert_live_project_status_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_freshness --lib` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_tui --test project_status_tui_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet live_project_status --lib` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_project_status_live_staged_inputs` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed.
- Impact: `rust/src/commands/dashboard/live_project_status.rs`, `rust/src/commands/datasource/project_status/live.rs`, `rust/src/commands/alert/project_status/live.rs`, `rust/src/commands/access/live_project_status.rs`, `rust/src/commands/sync/live_project_status_sync.rs`, `rust/src/commands/sync/live_project_status_promotion.rs`, `rust/src/commands/status/freshness.rs`, `rust/src/commands/status/tui/mod.rs`, `rust/src/commands/status/tui/render.rs`, `rust/tests/project_status_tui_rust_tests.rs`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The changes are additive and stay within domain-owned status modules, but future work should keep these heuristics traceable to existing inspect/live/export/preflight surfaces instead of turning project-status into an owner of new workflow semantics.
- Follow-up: keep the next pass focused on richer domain evidence, especially the dashboard live import/dry-run readiness follow-up, datasource live provider/placeholder readiness, alert live import, diff, and promotion readiness, and broader real-source timestamp freshness inside the existing project-status path, before adding any new top-level status commands or UI layers.

## 2026-03-29 - Current execution arrangement
- Summary: added one short execution-plan note above the detailed backlog and gap map so maintainers have a clear current order instead of continuing with scattered follow-ups. The plan treats domain-owned staged/live status producers as the mainline contract, puts `dashboard` first, `datasource` second, `sync/promotion` trust third, keeps `alert` behind those lanes unless a concrete consumer is blocked, and fixes the practical role split of `overview` as the staged artifact aggregator versus `project-status` as the project-wide status surface.
- Tests: not run. This is an architecture/planning documentation update only.
- Impact: `docs/internal/next-phase-execution-plan-2026-03-29.md`, `docs/overview-rust.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The change is documentation-only, but future updates should keep this file short and execution-oriented instead of turning it into a second roadmap.

## 2026-03-29 - Project-status live deepening and dedicated project-home TUI
- Summary: deepened the shared live project-status path instead of leaving it at a first-pass transport layer. `rust/src/commands/status/mod.rs` now accepts optional staged sync/promotion inputs during `project-status live`, consumes the richer live producers owned by dashboard, datasource, alert, access, sync, and promotion, stamps domain and overall freshness through `rust/src/commands/status/freshness.rs`, and exposes a first-class `interactive` output path backed by the dedicated project-home workbench in `rust/src/commands/status/tui/mod.rs` and `rust/src/commands/status/tui/render.rs`.
- Tests: added focused CLI parsing coverage for the new live staged-input flags, promoted the standalone project-status TUI tests into the main validation path, and revalidated the shared project-status renderer plus the existing overview consumer against the new wiring.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_project_status_command` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_project_status_live_staged_inputs` passed; `cargo test --manifest-path rust/Cargo.toml --quiet dispatch_routes_project_status_group_to_project_status_handler` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_tui --test project_status_tui_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet live_project_status --lib` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed.
- Impact: `rust/src/commands/status/mod.rs`, `rust/src/commands/status/freshness.rs`, `rust/src/commands/status/tui/mod.rs`, `rust/src/commands/status/tui/render.rs`, `rust/src/commands/dashboard/live_project_status.rs`, `rust/src/commands/datasource/project_status/live.rs`, `rust/src/commands/alert/project_status/live.rs`, `rust/src/commands/access/live_project_status.rs`, `rust/src/commands/sync/live_project_status.rs`, `rust/src/commands/sync/live_project_status_sync.rs`, `rust/src/commands/sync/live_project_status_promotion.rs`, `rust/src/commands/sync/mod.rs`, `rust/src/lib.rs`, `rust/src/cli/rust_tests.rs`, `rust/tests/project_status_tui_rust_tests.rs`, `docs/internal/project-status-architecture.md`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The changes stay additive at the shared contract level, but `project-status live` now has optional staged inputs and an interactive output surface, so future work must keep the staged/live distinction explicit and continue to avoid moving domain heuristics back into command-level glue.
- Follow-up: deepen domain-owned live heuristics, especially dashboard/datasource/alert drift and readiness, before adding more top-level project-status command branches or another consumer-specific status layer.

## 2026-03-29 - Project status staged rollout and project-home TUI
- Summary: completed the first full staged project-status rollout across the Rust mainline. Shared status types in `rust/src/commands/status/contract.rs` now carry domain/project freshness, ranked `topBlockers`, and project-level `nextActions`; domain-owned staged producers now exist for dashboard, datasource, alert, access, sync, and promotion; `rust/src/commands/status/overview/document.rs` aggregates those producers instead of owning their heuristics; and the overview interactive path now opens on a project-home landing that shows overall status, top action/handoff context, and domain rollup before dropping into the existing browser panes.
- Tests: expanded and repaired the focused overview regressions so they cover richer dashboard signal keys, domain freshness, project-level aggregation fields, and the project-home TUI interaction path while keeping the existing artifact/section contract checks in place.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_home_` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_dashboard_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_datasource_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_alert_project_status_domain` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_access_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_promotion_domain_status` passed.
- Impact: `rust/src/commands/status/contract.rs`, `rust/src/commands/dashboard/project_status.rs`, `rust/src/commands/datasource/project_status/staged.rs`, `rust/src/commands/alert/project_status/staged.rs`, `rust/src/commands/access/project_status.rs`, `rust/src/commands/sync/project_status.rs`, `rust/src/commands/sync/project_status_promotion.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/tui.rs`, `rust/src/commands/status/overview/tui_render.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The change is additive and staged-only, but it grows the shared project-status JSON contract and shifts more ownership to domain modules, so future edits need to preserve source attribution and avoid moving domain logic back into overview-level renderers.
- Follow-up: keep the next phase focused on second-pass domain producer depth and a separate live-status path; do not enlarge overview into the owner of staged/live heuristics.

## 2026-03-29 - Project-status command and first-pass live status
- Summary: added a dedicated `grafana-util project-status` command with explicit `staged` and `live` subcommands instead of leaving project-wide status only under `overview`. The shared aggregation logic for overall status, ranked blockers, and next actions now lives in `rust/src/commands/status/contract.rs`; `rust/src/commands/status/mod.rs` reuses the staged overview builders for `project-status staged`; and the first-pass live path now aggregates live dashboard search, datasource inventory, alert provisioning surfaces, and access list surfaces into the same shared `ProjectStatus` contract. Sync and promotion also now expose conservative live rows, but intentionally stay `unknown` until staged desired/bundle/mapping inputs are present.
- Tests: added focused project-status renderer/contract coverage and extended the CLI dispatch suite for the new root command, then revalidated overview to confirm the shared aggregation extraction did not regress the staged consumer.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_project_status_command` passed; `cargo test --manifest-path rust/Cargo.toml --quiet dispatch_routes_project_status_group_to_project_status_handler` passed; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_access_live_domain_status_reports` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_live_dashboard_domain_status_tracks_live_summary_and_datasource_reads` passed.
- Impact: `rust/src/commands/status/contract.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/dashboard/live_project_status.rs`, `rust/src/commands/datasource/project_status/live.rs`, `rust/src/commands/alert/project_status/live.rs`, `rust/src/commands/access/live_project_status.rs`, `rust/src/commands/sync/live_project_status.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/commands/status/tests.rs`, `docs/internal/project-status-architecture.md`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The shared `ProjectStatus` contract is still additive, but the new live command now depends on several conservative live read surfaces and intentionally represents some unreadable or unsupported areas as partial/unknown domain rows rather than silently omitting them.
- Follow-up: deepen live producers with richer drift/readiness signals where the domain already has owned live knowledge, and keep future TUI work centered on the shared project-status contract rather than letting `project-status` or `overview` grow separate status rules.

## 2026-03-29 - Access domain status producer
- Summary: moved staged access project-status derivation out of `overview_document.rs` and into a new access-owned producer in `rust/src/commands/access/project_status.rs`. The producer now aggregates the staged user, team, org, and service-account export bundles into one shared `ProjectDomainStatus`, carries explicit per-bundle source kinds and signal keys, and emits partial status when bundle kinds are missing or when all bundles are present but empty.
- Tests: added focused access-producer coverage for the partial/missing-bundle case and the all-bundles-present ready case, then added an overview regression that verifies the access domain row appears in the staged project-status document with the new access-owned mode and bundle attribution.
- Test Run: `cargo test --manifest-path rust/Cargo.toml --quiet build_access_domain_status_tracks_missing_bundle_kinds` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_access_export_sections` failed because unrelated `overview_tui.rs` test initializers are still missing `freshness`, `next_actions`, and `top_blockers` fields from the current shared project-status contract.
- Impact: `rust/src/commands/access/project_status.rs`, `rust/src/commands/access/mod.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The new producer is additive and keeps staged-only semantics, but the repo still has unrelated contract drift elsewhere that blocks a full crate test run.
- Follow-up: bring the unrelated overview TUI test fixtures in line with the current shared project-status contract when that scope is allowed.

## 2026-03-29 - Dashboard governance warning rows
- Summary: extended the dashboard domain-status producer so it can emit conservative staged governance warnings directly from the inspect summary document. The producer still treats orphaned datasources and mixed dashboards as blockers, but now also surfaces `riskRecordCount`, `highBlastRadiusDatasourceCount`, `queryAuditCount`, and `dashboardAuditCount` as source-attributable warning rows with a conservative review next action when the domain is otherwise ready or partial.
- Tests: added focused dashboard producer coverage for summary-only behavior and warning-enriched summary behavior.
- Test Run: `cargo test --quiet build_dashboard_domain_status -- --nocapture` failed in the current workspace because unrelated overview/access/sync status initializers are missing contract fields such as `freshness`, `next_actions`, and `top_blockers`.
- Impact: `rust/src/commands/dashboard/project_status.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The warning rows are additive and only appear when the staged summary document carries the corresponding counts.
- Follow-up: bring the unrelated project-status initializers in line with the current contract so the narrowed dashboard cargo test can pass end to end.

## 2026-03-29 - Overview architecture hardening
- Summary: hardened the Rust `overview` stack around the maintainer-facing data flow instead of adding more feature logic. Cross-cutting artifact-kind rules now live in `rust/src/commands/status/overview/kind.rs`, summary-fact projection moved to `rust/src/commands/status/overview/summary_projection.rs`, generic section assembly stays in `rust/src/commands/status/overview/sections.rs`, artifact-specific row builders moved to `rust/src/commands/status/overview/section_rows.rs`, the TUI render/layout path moved to `rust/src/commands/status/overview/tui_render.rs`, and `rust/src/commands/status/overview/document.rs` now renders text from artifact-level summary items instead of back-referencing the TUI-oriented `sections/views` projection. Project-status derivation also stays in one file but is now organized as ordered per-domain builders plus a small local spec layer instead of one growing branch-heavy function.
- Tests: reused the existing focused overview contract and CLI coverage because the work is structural and should preserve output semantics.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_overview_command` passed; `cargo test --manifest-path rust/Cargo.toml --quiet maybe_render_unified_help_from_os_args_handles_root_help_and_help_full_flags` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_all_sections` passed.
- Impact: `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/kind.rs`, `rust/src/commands/status/overview/summary_projection.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/sections.rs`, `rust/src/commands/status/overview/section_rows.rs`, `rust/src/commands/status/overview/tui.rs`, `rust/src/commands/status/overview/tui_render.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The contracts stay additive and the focused overview suite passed, but future overview changes should continue to preserve the single `args -> artifacts -> document -> render` path rather than reintroducing renderer-owned parsing or spreading artifact-kind policy back across modules.
- Follow-up: if `projectStatus` grows further, keep the next split inside `overview_document.rs` or a single adjacent status-specific helper instead of scattering domain heuristics across more files.

## 2026-03-29 - Shared project-status contract and sync producer
- Summary: moved the reusable project-status contract out of `overview` and into a crate-level `rust/src/commands/status/contract.rs`, then added the first domain-owned producer in `rust/src/commands/sync/project_status.rs`. The sync domain row now aggregates `sync-summary` and `bundle-preflight` evidence into one shared domain-status shape with `scope`, `mode`, `warningCount`, and `warnings`, and overview aggregation now treats sync as a real domain instead of also minting a separate `bundle-preflight` pseudo-domain row.
- Tests: updated focused overview contract assertions to cover the shared domain-status fields and the richer sync producer behavior for both plain `sync-summary` and bundle-preflight-backed sync status.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_sync_summary_domain_status` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_bundle_preflight_assessment_views` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_all_sections` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_overview_command` passed.
- Impact: `rust/src/commands/status/contract.rs`, `rust/src/commands/sync/project_status.rs`, `rust/src/commands/sync/mod.rs`, `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/tests.rs`, `rust/src/lib.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The JSON contract is additive in field shape but does change domain aggregation semantics by folding bundle-preflight evidence into the sync domain row, so downstream consumers that assumed `bundle-preflight` was a stable project-status domain id must adapt.
- Follow-up: add the next domain-owned producer in `dashboard`, then teach project-level aggregation to rank `topBlockers` and freshness without pulling those heuristics back into `overview`.

## 2026-03-29 - Dashboard domain status producer
- Summary: added `rust/src/commands/dashboard/project_status.rs` as the first dashboard-owned staged status producer and rewired overview aggregation to consume it. This keeps the first pass conservative by preserving the existing inspect-summary-based semantics for dashboard presence, orphaned datasource blockers, mixed-datasource dashboard blockers, and next-action text while moving ownership out of `overview_document.rs`.
- Tests: reused the focused overview regression suite because the intended change is ownership and layering, not a new user-visible dashboard-status heuristic.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_all_sections` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed.
- Impact: `rust/src/commands/dashboard/project_status.rs`, `rust/src/commands/dashboard/mod.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. The dashboard status output intentionally keeps the same visible staged semantics; the main change is ownership and future extension direction.
- Follow-up: deepen the dashboard producer with governance-backed warnings and exemplar risk/action data instead of adding those heuristics back into overview-level aggregation.

## 2026-03-29 - Project status architecture mini-spec
- Summary: added a dedicated maintainer mini-spec for whole-project progress/status support instead of continuing to treat `overview` as the architecture. The new note defines three layers: domain status producers, a shared project-status contract, and presentation consumers; makes staged/live separation explicit; and fixes the intended TUI model as `project home -> domain drill-down -> action handoff` so future work does not grow another artifact browser and call it project-wide status.
- Tests: Not applicable. This is an architecture/documentation change only.
- Test Run: Not run.
- Validation: reviewed existing `overview`, roadmap, backlog, TUI proposal, and maintainer architecture docs against the new design note.
- Impact: `docs/internal/project-status-architecture.md`, `docs/overview-rust.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This only adds a higher-level design map and maintainer routing.
- Follow-up: implement project-wide status support by adding domain status producers and a shared contract, not by pushing more unrelated logic into `rust/src/commands/status/overview/mod.rs`.

## 2026-03-29 - Project status producer gap list
- Summary: added a maintainer execution map for whole-project status work. The new gap-list note inventories which status-like producers already exist for dashboard, datasource, alert, access, sync, and promotion, classifies each domain as landed/partial/missing, and records the shortest safe next step for turning each one into a reusable domain status producer under the new project-status architecture.
- Tests: Not applicable. This is an architecture/documentation change only.
- Test Run: Not run.
- Validation: reviewed the current Rust domain contracts and maintainer docs with `rg`, `sed`, and `git diff` to align the gap list with visible code paths.
- Impact: `docs/internal/project-status-producer-gap-list.md`, `docs/internal/project-status-architecture.md`, `docs/overview-rust.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This only adds a planning/maintenance map.
- Follow-up: start implementation from a shared domain-status shape plus `sync` and `dashboard` producers, since those domains already have the strongest reusable staged/status surfaces.

## 2026-03-28 - Rust project overview command
- Summary: extended the Rust `grafana-util overview` command so the overview document now has two layers: the existing artifact-driven source of truth and a new additive project-status and section-model layer for UI consumers. The command still reuses dashboard inspect summary, datasource export inventory, alert export root index, access user/team/org/service-account export bundles, sync summary, bundle preflight, and promotion preflight documents/contracts, but now also emits a staged-only `projectStatus` contract with overall status plus per-domain blocker rows, derivation metadata (`reasonCode`, `sourceKinds`, `signalKeys`), and deterministic `nextActions`, emits section selection metadata plus view/item/detail rows aligned with the repo's existing browser/workbench vocabulary, can browse them with `--output interactive`, adds section-specific richer rows such as datasource inventory items, alert asset items, access roster items, sync resource items, promotion checks, and bundle assessment rows, splits the internals into `rust/src/commands/status/overview/mod.rs` orchestration, `rust/src/commands/status/overview/document.rs` document/render logic, `rust/src/commands/status/overview/artifacts.rs` artifact loading/building, `rust/src/commands/status/overview/sections.rs` section/view projection, and `rust/src/commands/status/overview/support.rs` shared JSON/loading helpers, and adds `docs/internal/overview-architecture.md` as the maintainer map for the design intent, module ownership, and extension rules behind that split.
- Tests: expanded overview contract/rendering coverage so JSON assertions cover the new `projectStatus` overall/domain shape, derivation metadata, next-action rows, the section-model shape, the interactive output selector, the bundle-preflight assessment view labels/items, and the extracted section-builder and artifact-builder paths, while preserving dashboard, datasource, alert, access, and sync-backed text rendering plus unified CLI parser/help coverage for the root command. The helper and document/render extractions stayed internal-only and required no test-surface changes.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all` passed; `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_overview_document_and_render_overview_text_for_all_sections` passed; `cargo test --manifest-path rust/Cargo.toml --quiet overview` passed; `cargo test --manifest-path rust/Cargo.toml --quiet access_export` passed; `cargo test --manifest-path rust/Cargo.toml --quiet parse_cli_supports_overview_command` passed; `cargo test --manifest-path rust/Cargo.toml --quiet dispatch_routes_overview_group_to_overview_handler` passed; `cargo test --manifest-path rust/Cargo.toml --quiet unified_help_mentions_alert_access_and_shims` passed; `cargo test --manifest-path rust/Cargo.toml --quiet maybe_render_unified_help_from_os_args_handles_root_help_and_help_full_flags` passed.
- Impact: `rust/src/commands/status/overview/mod.rs`, `rust/src/commands/status/overview/document.rs`, `rust/src/commands/status/overview/artifacts.rs`, `rust/src/commands/status/overview/sections.rs`, `rust/src/commands/status/overview/support.rs`, `rust/src/commands/status/overview/tui.rs`, `rust/src/commands/status/overview/tests.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/rust_tests.rs`, `rust/src/lib.rs`, `rust/src/commands/dashboard/mod.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The command remains additive and artifact-driven, but the new `projectStatus` layer now interprets staged artifact summaries into cross-domain readiness/blocker signals and suggested next actions, so future edits need to keep those derivations conservative, source-attributable, and explainable, and the split modules still need to stay aligned.
- Follow-up: keep `overview.rs` focused on CLI/run entrypoints only; next overview work should deepen the staged `projectStatus` heuristics or add a separate live-status path instead of pushing document, helper, or artifact code back into the main file.

## 2026-03-28 - Governance gate high blast radius policy
- Summary: extended dashboard governance gate policy with `datasources.forbidHighBlastRadius`, wired it through checked-rules output, and taught the evaluator to reject `datasourceGovernance` rows marked `highBlastRadius`. This turns the inspect-only concentration signal into an enforceable policy input for CI and promotion paths.
- Tests: added a focused governance-gate regression for the new datasource policy and updated existing checked-rules snapshots that now include the new defaulted policy field.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet evaluate_dashboard_governance_gate_enforces_datasource_policy_rules` passed; `cargo test --manifest-path rust/Cargo.toml --quiet evaluate_dashboard_governance_gate_enforces_high_blast_radius_datasource_policy` passed; `cargo test --manifest-path rust/Cargo.toml --quiet evaluate_dashboard_governance_gate_enforces_query_thresholds_and_warning_policy` passed; `cargo test --manifest-path rust/Cargo.toml --quiet evaluate_dashboard_governance_gate_enforces_query_and_dashboard_complexity_rules` passed; `cargo test --manifest-path rust/Cargo.toml --quiet render_dashboard_governance_gate_result_lists_violations_and_warnings` passed; `cargo test --manifest-path rust/Cargo.toml --quiet run_dashboard_governance_gate_writes_json_output_file` passed; `cargo test --manifest-path rust/Cargo.toml --quiet run_dashboard_governance_gate_reads_yaml_policy_file` passed.
- Impact: `rust/src/commands/dashboard/governance_gate_rules.rs`, `rust/src/commands/dashboard/governance_gate_rules_policy.rs`, `rust/src/commands/dashboard/governance_gate_rules_evaluation_apply.rs`, `rust/src/commands/dashboard/export_focus_governance_rule_datasource_routing_complexity_rust_tests.rs`, `rust/src/commands/dashboard/export_focus_governance_rule_threshold_audit_cost_rust_tests.rs`, `rust/src/commands/dashboard/export_focus_governance_output_rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The new policy flag defaults off, but any tooling that snapshots `checkedRules` must accept the added `forbidHighBlastRadius` field.
- Follow-up: if needed, add a numeric gate threshold such as allowed high-blast-radius datasource count instead of only a boolean forbid switch.

## 2026-03-28 - Datasource concentration threshold
- Summary: added a conservative `highBlastRadius` threshold on top of the new datasource blast-radius fields. Governance summary now counts high-blast-radius datasources, datasource governance rows expose the boolean directly, and offline dependency usage mirrors the same threshold so dependency and governance outputs stay aligned.
## 2026-03-31 - Rust profile/workspace usability foundation
- Summary: implemented the first Rust usability layer around repo-local profiles. The CLI now supports `grafana-util profile list/show/init` and a `grafana-util.yaml` config file for named live connection defaults. Dashboard, datasource, access, alert, and `status live` now accept `--profile` and merge selected profile values into existing live auth/transport behavior without replacing explicit CLI flags or the older `GRAFANA_*` fallback path.
- Tests: added/expanded parser, help, and runtime tests for the new profile namespace and the shared profile loader; revalidated the affected live-command suites plus the full Rust test suite.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`; `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet cli_rust_tests`; `cargo test --manifest-path rust/Cargo.toml --quiet dashboard`; `cargo test --manifest-path rust/Cargo.toml --quiet datasource`; `cargo test --manifest-path rust/Cargo.toml --quiet access`; `cargo test --manifest-path rust/Cargo.toml --quiet alert`; `cargo test --manifest-path rust/Cargo.toml --quiet project_status_cli_rust_tests`; `cargo test --manifest-path rust/Cargo.toml --quiet`
- Impact: `rust/src/commands/config/profile/config.rs`, `rust/src/commands/config/profile/mod.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/commands/dashboard/cli_defs_shared.rs`, `rust/src/commands/dashboard/dashboard_runtime.rs`, `rust/src/commands/access/access_cli_shared.rs`, `rust/src/commands/access/access_cli_runtime.rs`, `rust/src/commands/alert/cli/mod.rs`, `rust/src/commands/status/mod.rs`, `rust/src/commands/status/support.rs`, focused Rust tests, `docs/user-guide.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. This is a cross-cutting but bounded UX change; the risk is mainly in live auth/transport precedence rather than resource semantics. The current scope intentionally stops at live connection defaults and does not yet push staged/export path presets through the wider command graph.

- Tests: expanded governance document coverage for the new threshold signal, plus dependency-contract and dependency-render checks for `highBlastRadius` true/false behavior and TUI fixture updates for the richer summary shape.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_export_inspection_governance_document_adds_dashboard_datasource_edges` passed; `cargo test --manifest-path rust/Cargo.toml --quiet render_governance_table_report_displays_sections` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_export_inspection_governance_document_groups_core_family_dependency_rows` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_offline_dependency_contract_reports_unique_dashboard_and_panel_counts` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_offline_dependency_contract_surfaces_richer_usage_and_orphan_details` passed; `cargo test --manifest-path rust/Cargo.toml --quiet render_dependency_table_report_normalizes_empty_orphan_cells` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_inspect_workbench_document_adds_dependency_coverage_views` passed.
- Impact: `rust/src/commands/dashboard/inspect_governance_rows.rs`, `rust/src/commands/dashboard/inspect_governance_coverage.rs`, `rust/src/commands/dashboard/inspect_governance_document.rs`, `rust/src/commands/dashboard/inspect_governance_render.rs`, `rust/src/commands/dashboard/inspect_workbench_content.rs`, `rust/src/commands/dashboard/inspect_workbench_state.rs`, `rust/src/commands/dashboard/inspect_governance_risk_spec.rs`, `rust/src/commands/dashboard/inspection/dependency_contract.rs`, `rust/src/commands/dashboard/inspect_dependency_render.rs`, `rust/src/commands/dashboard/inspect_governance_document_rust_tests.rs`, `rust/src/commands/dashboard/inspect_live_tui_rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The threshold is intentionally conservative and internal to inspect output, but downstream consumers that treat datasource governance rows as a fixed schema now need to tolerate the extra summary and boolean fields.
- Follow-up: if this proves useful, promote the threshold into governance gate policy input instead of leaving it as inspect-only guidance.

## 2026-03-28 - Datasource blast-radius outputs
- Summary: extended dashboard inspect so datasource governance and offline dependency usage expose explicit blast-radius signals instead of only raw UID counts. Datasource rows now carry folder count, cross-folder state, folder paths, and dashboard titles, and those fields are rendered through governance tables, dependency tables, and the inspect workbench.
- Tests: updated governance document coverage and workbench fixture coverage, plus dependency-contract and dependency-render assertions for the richer datasource usage shape.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_export_inspection_governance_document_adds_dashboard_datasource_edges` passed; `cargo test --manifest-path rust/Cargo.toml --quiet render_governance_table_report_displays_sections` passed; `cargo test --manifest-path rust/Cargo.toml --quiet render_dependency_table_report_normalizes_empty_orphan_cells` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_offline_dependency_contract_reports_unique_dashboard_and_panel_counts` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_offline_dependency_contract_surfaces_richer_usage_and_orphan_details` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_inspect_workbench_document_adds_dependency_coverage_views` passed.
- Impact: `rust/src/commands/dashboard/inspect_governance_rows.rs`, `rust/src/commands/dashboard/inspect_governance_coverage.rs`, `rust/src/commands/dashboard/inspect_governance_render.rs`, `rust/src/commands/dashboard/inspect_workbench_content.rs`, `rust/src/commands/dashboard/reference_models.rs`, `rust/src/commands/dashboard/inspection/dependency_contract.rs`, `rust/src/commands/dashboard/inspect_dependency_render.rs`, `rust/src/commands/dashboard/inspect_governance_document_rust_tests.rs`, `rust/src/commands/dashboard/inspect_live_tui_rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The change only enriches existing inspect models, but downstream consumers that assume fixed table columns or a narrow JSON field set for datasource usage/governance rows will need to tolerate the added blast-radius fields.
- Follow-up: use the new folder/dashboard blast-radius data to add stronger dependency concentration summaries or governance thresholds, instead of growing more datasource-family-specific parser detail first.

## 2026-03-28 - Loki pipeline field hints
- Summary: tightened Loki selector matcher extraction so only stream-selector label matchers are recorded there, then added conservative pipeline field extraction for obvious parser predicates and `unwrap` targets. Shared inspect query rows can now surface field hints such as `status`, `level`, and `duration_ms` without expanding the report surface.
- Tests: added focused Loki analyzer and end-to-end query-report extraction coverage.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check` passed; `cargo test --manifest-path rust/Cargo.toml --quiet dispatch_query_analysis_extracts_loki_pipeline_field_hints` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_export_inspection_query_report_extracts_loki_pipeline_field_hints` passed; `cargo test --manifest-path rust/Cargo.toml --quiet build_export_inspection_query_report_matches_core_family_query_row_contract` passed.
- Impact: `rust/src/commands/dashboard/inspect_analyzer_loki.rs`, `rust/src/commands/dashboard/export_focus_report_query_family_analysis_rust_tests.rs`, `rust/src/commands/dashboard/export_focus_report_query_family_extraction_rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low-to-moderate. The change is intentionally conservative and limited to Loki inspection analysis, but overly broad predicate matching would affect downstream dependency and governance summaries for Loki-backed dashboards.
- Follow-up: extend Loki analysis only where the signals remain explainable, for example parser-specific extracted fields or richer stage semantics beyond the new predicate and `unwrap` hints.

## 2026-03-28 - Rust mainline documentation refresh
- Summary: refreshed the live internal docs so they match the Rust-only mainline, shortened the AI trace files to current-only snapshots, and tightened the roadmap/backlog/TODO wording around the active Rust maintenance direction.
- Tests: Not applicable.
- Test Run: Not run.
- Validation: reviewed the owned docs with `rg`, `sed`, and `git diff`.
- Impact: `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`, `docs/internal/project-roadmap.md`, `docs/internal/maintainer-backlog-2026-03-28.md`, `TODO.md`, `docs/internal/archive/ai-status-archive-2026-03-28.md`, `docs/internal/archive/ai-changes-archive-2026-03-28.md`
- Rollback/Risk: low. This only changes documentation structure and wording.
- Follow-up: none.
## 2026-03-29 - Project-level stop/continue closeout
- Summary: closed the bounded follow-through pass at the project level. The current execution review, maturity review, next-phase note, and gap list now all agree that no domain-owned depth lane remains active by default; `dashboard`, `datasource`, `alert`, `access`, `sync`, and `promotion` are all stop-for-now unless a concrete consumer proves a missing decision-critical signal. `overview` and `project-status` remain thin consumers rather than targets for more derivation work.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/current-execution-review-2026-03-29.md`, `docs/internal/domain-producer-maturity-review-2026-03-29.md`, `docs/internal/next-phase-execution-plan-2026-03-29.md`, `docs/internal/project-status-producer-gap-list.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is a documentation-only closeout that narrows current execution intent and makes it harder to reopen depth lanes without a concrete consumer-driven reason.
## 2026-03-29 - Roadmap and backlog alignment after producer closeout
- Summary: aligned the roadmap stack to the project-level closeout decision. `docs/internal/project-roadmap.md`, `docs/internal/maintainer-backlog-2026-03-28.md`, and `TODO.md` now match the current execution stance: keep domain-owned producers stable by default, reopen lanes only from concrete consumer pressure, keep `overview` / `project-status` thin, and treat broader analysis or packaging work as exploratory instead of active roadmap lanes.
- Tests: Not run. Documentation-only update.
- Impact: `docs/internal/project-roadmap.md`, `docs/internal/maintainer-backlog-2026-03-28.md`, `TODO.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low. This is a documentation-only alignment update that reduces conflicting maintainer guidance across the repo.
## 2026-03-30 - Shared TUI presentation pass for pane-based interactive surfaces
- Summary: unified the visible shell treatment across the main tab-driven interactive screens. `overview` now renders a single upper summary block instead of a duplicated project-home block, footer controls use aligned fixed-width cells, and focus state is consistently highlighted with blue chips across the main pane-based TUI surfaces. The dashboard inspect workbench was also pulled into the same treatment and now has a focused render smoke test so `inspect-export --interactive` cannot silently regress into a blank-looking shell. Interactive export/live inspect also print explicit progress steps before the workbench opens, so long-running report/governance analysis no longer looks like a dead terminal.
- Tests: Focused Rust TUI render tests.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet project_home_lines_surface_status_and_next_action`; `cargo test --manifest-path rust/Cargo.toml --quiet interactive_render_starts_on_project_home_surface`; `cargo test --manifest-path rust/Cargo.toml --quiet summary_lines_surface_focus_and_mode`; `cargo test --manifest-path rust/Cargo.toml --quiet cli_review_tui_rust_tests`; `cargo test --manifest-path rust/Cargo.toml --quiet inspect_workbench_render_surfaces_header_panes_and_footer`; `cargo test --manifest-path rust/Cargo.toml --quiet build_inspect_workbench_document_adds_dependency_coverage_views`
- Impact: `rust/src/common/tui/shell.rs`, `rust/src/commands/status/overview/tui_render.rs`, `rust/src/commands/status/overview/tui.rs`, `rust/src/commands/status/tui/render.rs`, `rust/src/common/browser/session.rs`, `rust/src/commands/dashboard/topology_tui.rs`, `rust/src/commands/dashboard/impact_tui.rs`, `rust/src/commands/dashboard/governance_gate_tui.rs`, `rust/src/commands/dashboard/inspect_workbench_render.rs`, `rust/src/commands/dashboard/inspect_orchestration.rs`, `rust/src/commands/dashboard/inspect_live.rs`, `rust/src/commands/sync/audit_tui.rs`, `rust/src/commands/sync/review_tui_helpers.rs`, `rust/src/commands/datasource/browse/render.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. This is presentation work only, but it touches several renderers, so regressions would show up as TUI layout drift rather than semantic behavior changes.

## 2026-03-30 - Alert scaffold naming and live operator examples
- Summary: tightened the new alert management lane so scaffold generation now respects the requested resource name, then refreshed the operator guides with locally validated Grafana `12.4.1` examples. `alert new-rule`, `new-contact-point`, and `new-template` now seed identity fields from `--name` instead of generic placeholders, and the user guides now show a full example covering export, import dry-run, add-rule plan/apply, and prune-based delete-rule plan/apply.
- Tests: focused Rust alert tests plus live Docker Grafana command validation for the documented flows.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`; `cargo test --manifest-path rust/Cargo.toml --quiet alert -- --test-threads=1`; live commands against Docker Grafana `12.4.1`: `alert export`, `alert import --dry-run --json`, `alert init`, `alert new-rule`, `alert new-contact-point`, `alert new-template`, `alert plan --output json`, `alert apply --output json`
- Impact: `rust/src/commands/alert/mod.rs`, `rust/src/commands/alert/support/mod.rs`, `rust/src/commands/alert/runtime_support.rs`, `rust/src/commands/alert/tests/mod.rs`, `docs/user-guide.md`, `docs/user-guide-TW.md`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: low to moderate. The scaffold naming change is operator-facing and improves correctness, but any external tooling that had come to rely on the old generic placeholder identities inside scaffold files would need to tolerate the new name-aligned defaults.

## 2026-03-30 - Alert authoring commands and managed route overwrite behavior
- Summary: completed the first wired alert V2 authoring layer without changing the core live execution model. The alert CLI now exposes `add-rule`, `clone-rule`, `add-contact-point`, `set-route`, and `preview-route` as desired-state authoring commands, all with `--dry-run` support. These commands only write or preview staged files under the desired tree; they do not call Grafana APIs directly.
- Tests: expanded parser/help coverage and added runtime smoke tests for authored rule writes, dry-run clone behavior, and in-place managed route replacement.
- Test Run: `cargo fmt --manifest-path rust/Cargo.toml --all`; `cargo fmt --manifest-path rust/Cargo.toml --all --check`; `cargo test --manifest-path rust/Cargo.toml --quiet alert -- --test-threads=1`; `cargo test --manifest-path rust/Cargo.toml --quiet`
- Impact: `rust/src/commands/alert/mod.rs`, `rust/src/commands/alert/cli/mod.rs`, `rust/src/commands/alert/support/mod.rs`, `rust/src/commands/alert/runtime_support.rs`, `rust/src/commands/alert/tests/mod.rs`, `rust/src/cli/help_examples.rs`, `rust/src/cli/rust_tests.rs`, `docs/internal/ai-status.md`, `docs/internal/ai-changes.md`
- Rollback/Risk: moderate. The new authoring surface is intentionally bounded, but it introduces a managed notification-policy lane whose overwrite semantics must stay explicit. The design assumes tool-owned route reruns replace the same managed route rather than merging with hand-edited fields.
- Added a file-first Rust dashboard authoring lane with `dashboard get`, `dashboard clone-live`, `dashboard patch-file`, and `dashboard publish`; the new commands fetch or mutate single-dashboard drafts locally and route publish through the existing import pipeline rather than a second live apply engine.
- Added `dashboard review` as the lightweight local authoring checkpoint for single-dashboard drafts; it distinguishes wrapped vs bare files, reports folder/message/id state, surfaces strict validation blockers, and emits a suggested next action in both text and JSON modes.
