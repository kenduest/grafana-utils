.\" Generated by scripts/generate_manpages.py from docs/commands/en/.
.TH GRAFANA-UTIL-DASHBOARD-EXPORT 1 "2026-04-03" "grafana-util 0.9.0" "User Commands"
.SH NAME
grafana\-util\-dashboard\-export \- Export dashboards to raw/, prompt/, and provisioning/ files, with optional history/ artifacts.
.SH SYNOPSIS
.B grafana-util dashboard export [\fIOPTIONS\fR]
.SH DESCRIPTION
Export dashboards to raw/, prompt/, and provisioning/ files, with optional history/ artifacts.
.PP
Use this when you need a local export tree for later import, review, diff, or file\-provisioning workflows. Add \-\-include\-history when you also need dashboard revision\-history artifacts under each exported org scope. The prompt/ lane is for Grafana UI import, not dashboard API import; use dashboard raw\-to\-prompt when you need to convert ordinary or raw dashboard JSON into prompt JSON.
.SH BEFORE / AFTER
.IP \(bu 2
Before: export is a one\-off action, and you only discover later whether the tree is reviewable or reusable.
.IP \(bu 2
After: export becomes the first artifact in a repeatable workflow that can feed inspect, diff, dry\-run import, and Git review.
.SH COMMON OPTIONS
.TP
.B \-\-output\-dir
target directory for the export tree.
.TP
.B \-\-org\-id
export one explicit Grafana org.
.TP
.B \-\-all\-orgs
export each visible org into per\-org subdirectories. Prefer Basic auth.
.TP
.B \-\-flat
write files directly into each export variant directory.
.TP
.B \-\-overwrite
replace existing export files.
.TP
.B \-\-without\-dashboard\-raw, \-\-without\-dashboard\-prompt, \-\-without\-dashboard\-provisioning
skip a variant.
.TP
.B \-\-include\-history
write dashboard revision\-history artifacts under a history/ subdirectory for each exported org scope.
.TP
.B \-\-provisioning\-provider\-name, \-\-provisioning\-provider\-org\-id, \-\-provisioning\-provider\-path
customize the generated provisioning provider file.
.TP
.B \-\-provisioning\-provider\-disable\-deletion, \-\-provisioning\-provider\-allow\-ui\-updates, \-\-provisioning\-provider\-update\-interval\-seconds
tune provisioning behavior.
.TP
.B \-\-dry\-run
preview what would be written.
.SH SUCCESS CRITERIA
.IP \(bu 2
a raw/ tree exists for API replay and deeper inspection
.IP \(bu 2
a prompt/ tree exists when you need a cleaner handoff for UI\-style import
.IP \(bu 2
a history/ tree exists under each exported org scope when you pass \-\-include\-history
.IP \(bu 2
the export tree is stable enough to commit, diff, or inspect before mutation
.SH FAILURE CHECKS
.IP \(bu 2
if dashboards are missing, check org scope before suspecting the exporter
.IP \(bu 2
if multi\-org output looks partial, check whether the credential can really see every org
.IP \(bu 2
if expected history artifacts are missing, confirm that you passed \-\-include\-history and are checking the right org scope
.IP \(bu 2
if the next step is import, confirm whether you should continue from raw/ or prompt/
.SH EXAMPLES
.PP
Export dashboards to raw/, prompt/, and provisioning/ files, with optional history/ artifacts.
.EX
grafana-util dashboard export --profile prod --output-dir ./dashboards --overwrite
.EE
.SH SEE ALSO
\fBgrafana-util\fR(1),
\fBgrafana-util-dashboard\fR(1)
